import styled from 'styled-components'
import Layout, { ComponentArea, Spacer, Heading } from './layout'
import { carouselSections, missionSections } from './consts/sampleSections'
import Carousel from '../../src/components/carousel'
import Mission from '../../src/components/mission'

<Heading>Carousel</Heading>

<Spacer size='l' />

<ComponentArea>
  <Carousel sections={carouselSections} />
</ComponentArea>

<Spacer size='l' />

```jsx
import React from 'react'
import { Carousel } from '@party-opu/funii-web-components'

const Page = () => (
  <React.Fragment>
    <Carousel sections={sections} />
  </React.Fragment>
)

export default Page
```

<Spacer size='xxl' />

<Heading>Mission</Heading>

<Spacer size='l' />

<ComponentArea>
  <Mission sections={missionSections} />
</ComponentArea>

<Spacer size='l' />

```jsx
import React from 'react'
import { Mission } from '@party-opu/funii-web-components'

const Page = () => (
  <React.Fragment>
    <Mission sections={sections} />
  </React.Fragment>
)

export default Page
```

export default ({ children }) => <Layout>{children}</Layout>
